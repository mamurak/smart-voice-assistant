# Smart Voice Assistant — web UI (static + config/proxy server).
# Build (ImageStream + binary Docker BuildConfig) and runtime objects.
# Apply with:  oc apply -f webui.yaml -n <namespace>
# then:        oc start-build smart-voice-assistant --from-dir=.. --follow -n <namespace>
---
# Endpoint wiring — edit these to point at your STT / LLM / TTS services.
apiVersion: v1
kind: ConfigMap
metadata:
  name: smart-voice-assistant-config
  labels: { app: smart-voice-assistant }
data:
  SVA_APP_TITLE: "Smart Voice Assistant"
  # Prepopulated for the default `voice-assistant` namespace. The install
  # scripts (full-install / app-install) rewrite these via `oc set data` with
  # the namespace you actually deployed to, so a different namespace is handled
  # automatically — but if you apply this file by hand into another namespace,
  # replace `voice-assistant` below with it. You can also override in Settings.
  SVA_STT_MODEL: "whisper-large-v3"
  SVA_STT_ENDPOINT: "http://whisper-large-v3-predictor.voice-assistant.svc.cluster.local:8080/v1"
  SVA_LLM_MODEL: "ministral-3-3b-instruct"
  SVA_LLM_ENDPOINT: "http://ministral-3-3b-instruct-predictor.voice-assistant.svc.cluster.local:8080/v1"
  SVA_TTS_MODEL: "supertonic-3"
  SVA_TTS_ENDPOINT: "http://supertonic.voice-assistant.svc.cluster.local:7788/v1/tts"
  SVA_TTS_API: "native"
  SVA_TTS_FORMAT: "wav"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-voice-assistant
  labels: { app: smart-voice-assistant }
  annotations:
    image.openshift.io/triggers: |
      [{"from":{"kind":"ImageStreamTag","name":"smart-voice-assistant:latest"},"fieldPath":"spec.template.spec.containers[?(@.name==\"web\")].image"}]
spec:
  replicas: 1
  # Recreate (not RollingUpdate): a config change restarts the pod, and we never
  # want the old pod (with stale/empty endpoints) sitting behind the Route next
  # to the new one — that races the post-install component test onto the old pod.
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: smart-voice-assistant }
  template:
    metadata:
      labels: { app: smart-voice-assistant }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: web
          image: quay.io/hasan_badawy_ai/smart-voice-assistant:v1.0.0
          imagePullPolicy: Always     # :latest — always re-pull the new push on restart
          ports:
            - { name: http, containerPort: 8080 }
          envFrom:
            - configMapRef: { name: smart-voice-assistant-config }
          env:
            - { name: HOST, value: "0.0.0.0" }
            - { name: PORT, value: "8080" }
          readinessProbe:
            httpGet: { path: /api/health, port: http }
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/health, port: http }
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 256Mi }
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
---
apiVersion: v1
kind: Service
metadata:
  name: smart-voice-assistant
  labels: { app: smart-voice-assistant }
spec:
  selector: { app: smart-voice-assistant }
  ports:
    - { name: http, port: 8080, targetPort: http }
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: smart-voice-assistant
  labels: { app: smart-voice-assistant }
  annotations:
    # CPU TTS synth can take longer than the router's 30s default → 504.
    haproxy.router.openshift.io/timeout: 120s
spec:
  to: { kind: Service, name: smart-voice-assistant }
  port: { targetPort: http }
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
